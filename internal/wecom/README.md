# WeCom domain placeholder

首批只允许实现：

- Desktop SSO；
- Sidebar Staff OAuth；
- JSSDK `config` / `agentConfig`；
- external contact context；
- callback verify/decrypt；
- Provider read facts。

WeCom 不拥有 Customer、Profile、Order、Tag 或发送业务状态。具体实现必须等待 PRD 中 Wave 2 的合同和 Journey。
