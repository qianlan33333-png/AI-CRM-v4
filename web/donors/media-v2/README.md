# PR02 frontend donor snapshot

The files below are copied byte-for-byte from the frozen v2 donor. They are
kept outside `web/src` so the v3 frontend and central webshell remain
unchanged. Do not edit these files in place; use the manifest and source
commit when selecting the later Terra-owned integration boundary.
