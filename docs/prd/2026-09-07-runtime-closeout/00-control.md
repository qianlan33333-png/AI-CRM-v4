# 四板块运行收口总控

## 当前状态快照（2026-09-08 13:15 CST）

下方早期条目为审查记录，以本表和后续更新为准；不能把历史待办或旧 HEAD 的绿色当作当前结果。

| 项目 | 当前准确状态 | 尚待完成 |
| --- | --- | --- |
| 客户同步 / 自动化 | #185 已部署；最新每日同步 succeeded，观察 23,493 条、失败 0。自动化 limited / 5000 已生产回读 | 人工业务验收，未进行测试群发 |
| 问卷外推 | #187 已部署；全局 Provider、两个目标及 7 个来源唯一映射问卷配置均启用 | 人工正常新提交后的真实投递验收；没有重推历史答卷 |
| 配置中心 | #190 / #194 已随 `af1770722414d747bf7103cb08af490ffabec37d` 部署。正常管理员 API 返回标准 12 类、revision=0/environment_default，OAuth、AI、自动化、问卷外推等实际值正确 | 最终版本部署后重复核对；真实页面操作人工验收 |
| HXC 发布核对 | #195 已合并，生产身份写入恢复；main CI `34183357621` 部署成功 | #197 用户独立授权普通部署不再执行 HXC 业务验收；首次激活或不兼容规则变更保留手动验收入口 |
| 群运营 | #186 准确 HEAD `ac9a8d238178bf457518caa3c0e1981760c627e6` 全 CI `34183498798` 通过，合并为 `6deb8b386e153070a6fab3737b122be9fe9406f5` | 部署、目录/发送配置应用、正常 V3 目录同步；真实发送人工另验 |
| 运营 Runner | #193 最终审核 HEAD `b8e4b62` 全 CI 通过，已合并；两个正式发行二进制及缺失/篡改/回滚合同已进入主线，具备打包条件 | 仍须随最终主线部署；实际执行机器、目录与 socket 尚未确认，未安装、未创建业务任务 |
| 侧边栏 | #192 正在更新最终主线 `1778225e308504a40679033f08853cdc2f723eef`；正式 PRD/证据、客户 scope、未知接受结果稳定幂等、真实 replay、PG 接受/到期事务、素材严格查询与标准 pager、唯一冻结 image loader、UTC 回放及完整发布闭包均已纳入 | 最终准确 HEAD 的 Linux Chromium 与完整 CI 待验证；本机 DevTools SIGABRT 不算通过；真实企微分享仍须人工验收 |
| 会话存档 | Provider SDK 拉取/解密成功，生产启用状态已回读 | 官方通知回调 URL / AgentID 未确认，尚无通知驱动入库证据 |
| AI 调用端 | CRM OAuth、REST /open/v1 与 MCP 六项能力读取已生产验证，AI dispatch=true | 上游 AI 服务部署位置未明确；真实计划由人工提交验收 |

后续次序：最终主线为 `1778225e308504a40679033f08853cdc2f723eef`，#198 已全绿合并；当前只推进 #192 最终准确 HEAD 审查及 required check，严格主线保护不绕过。#197 的 main run `34185705197` 在 2026-09-08 05:05Z 达到原 60 分钟上限后取消，未安装；#198 的 main run `34190169988` 也在安装前取消。当前现网经 `/readyz` 与安装目录核对仍为 `af177072`，最终版本待 #192 门禁通过后发布。PR #189 保留前置 PRD/旧仓核查审核记录，需求及脱敏证据归档进最终侧边栏 PR #192，不混入其他板块业务实现，不覆盖已合并的配置中心 PRD。

运行配置核对边界：12 类目录及 effective settings 来自正常登录后的 runtime-catalog；application 历史记录命中不能独自证明当前进程。最终配置应用还须核 API/effects 当前 PID、二进制和 release，普通 worker 为 timer 驱动的 oneshot，以新一轮正常执行成功及快照一致核对，不要求空闲时常驻进程。

用户要求：解决群运营、自动化运营、会话存档，并恢复旧配置中心真实能力。主任务只做 PRD、审核、协调、生产配置与发布；实现委派执行任务。每板块独立完整 PR，沿用已有成果。最多三个开发任务同时运行；群运营沿用现有任务，先并行配置中心与自动化，会话存档先由主任务只读准备，空位后开发。

交付状态分别列实现、测试、审核、合并、部署、配置应用、真实业务验收。现有 AI 生产发送及 V1 调用方不可回退。不开启未审阅旧任务或给历史终态补发。

共享契约：配置中心拥有版本化非秘密业务配置及发布记录；平台环境拥有部署凭据引用。领域通过 config/port 或 composition 注入配置快照，不读他域表。现有静态凭据若暂需受控重启，UI 必须如实显示待应用，不能把保存当生效。配置执行者协调配置类型和 composition 公共接线；自动化、群与存档先保持自身代码边界并提交最小装配差异。

## 固定约束与验收
旧供体 AI-CRM@dd8d60dd8ddb983aca2ec88cc9e65a9f7563f79f，只读检出 /private/tmp/aicrm-release-fallback-donors/sidebar；V3 起点 cc4bb8c7e32d4d7d89f6a9a0b0a928bb562584bc，实施前刷新 origin/main。先读 skills/aicrm-v3-development/SKILL.md。实现者在本 PRD 附录逐字段/命令列出原样复用、Go 等价、V3 已有和待补齐的来源证据，之后再编码。前端照搬供体结构、样式、操作顺序，允许 JS 转 TS，仅在 Host/Adapter 做接口适配；遵循已上线单源供体清单机制，不整目录复制。
每板块完整 PR：实际后端/页面/运行装配、必要迁移、架构/编译/专项、真实 PostgreSQL 事务并发恢复、Chromium 操作、协议测试及完整 CI。先修具体失败再跑全量，不削弱冻结或业务断言。历史数据迁移不在本轮。部署只发布审核通过的准确 HEAD，核 API/Worker 版本、dist 和配置实际生效，保留回滚路径。生产网络配置及凭据只经受保护文件；日志、代码、文档不含秘密或 PII。真实发送/支付需有明确业务目标与内容，不为验收擅自发送。
不恢复旧 56 条机器接口，不新建 OneID 匹配、队列、Worker/重试内核，不顺带开发 Campaign、客服权限或客户个人免打扰。发现本板块旧真实流程依赖未覆盖项，报告具体证据，不伪造成功。

## 用户追加范围
客户每日同步修复优先并行；自动化、存档、问卷外推、运营 Runner 和实际 AI 调用端全部接通。客户同步完成后，当前开发三项为问卷外推、群运营、配置中心；其他先由根准备生产配置和旧来源证据，空位后逐板块调度。每项独立验收，不把凭据发放/页面可开当全部接通。

## 2026-09-07 22:46 接通核对

- 客户同步：PR #185 已通过准确 HEAD `5575dbfda047bf61cea4c45a8cbda976253477cf` 完整 CI（run 34130816094）及真实 PG16 / race，审核后合并并部署为 `3f5ea38c702b91e37746c2ad1b7bf4f5174e5a2a`。正常管理员入口发起 run 6，22:42:24 成功结束；43 位员工，23,493 条关系观察，32 activated、23,461 already_linked，conflict/terminal_failed/stale 均 0，projected=23,493。计数恒等式与逐项 outcome 对账通过。这是正常 Provider 同步，不是旧数据库历史导入。
- 自动化：生产 API/effects-worker 已启用 limited 模式，固定话术发送权限已配置，max recipients=5000。管理员配置读取返回 revision=0、environment_default、5000。启用前无策略、无待执行 River 工作；没有生产发送。`3f5ea38` 部署后重新读取进程确认 limited / 5000 保留，AI dispatch 与微信支付 enabled 也保留。人工业务流程仍待现场验收。
- 存档：旧环境凭据是 `secretref`，已通过旧运行用户读取实际生效值并安全准备至 V3；SDK 二进制摘要一致、隔离 Runner health 与鉴权通过。真实 GetChatData 返回 301042，待企微会话存档白名单新增 V3 IP；仍未开启消息处理，不能声称增量在线。后续还须核通知 URL/实际通知收据及游标事务。
- 配置：已从旧生产运行设置和安全引用准备 73 个相关配置键至 V3 受保护文件，仅作为部署来源，V3 运行不连接旧配置数据库。配置中心代码任务仍进行中。
- 问卷外推：旧已启用问卷的真实目标与 HMAC 来源已找到，目标含秘密 query；内容只在受保护文件和进程内传递。独立执行任务核对 target 配置解析、绑定、身份与 PG/协议测试。
- Runner / AI 调用端：CRM 侧 V1 调用凭据已存在；旧服务器未发现已登记 Runner、心跳或机器绑定配置。实际执行端仍需定位/明确，不能以创建凭据代替在线心跳与真实连接。

上述配置準备不包含历史业务数据导入，不会启用旧系统的待发送任务。


## 23:05 审核补充

- 群运营 PR #186 仍在修正审核发现的排期来源与负责人变更问题，未合并、未部署、未启用 Provider 写。新日历节点与旧 relative delay 使用显式持久标识区分；负责人变更必须同一业务事务，禁止前端多请求留下半完成成员关系。
- 配置中心修正了审核指出的 native JSON 文本值二次解析；还在完成真实 HTTP/PG/Chromium 发布与回读测试，未上线。
- `3f5ea38` 的 main CI run 34133094813 在 30 分钟总时限后 cancelled；日志显示完整 make check 通过，race 的 cmd/aicrm、Access、Identity 包仍成功推进，未见测试失败结论。客户修复的准确 PR HEAD 完整 CI 已通过且与生产树一致；主线超时不标记为绿色。共享时限修复保持独立小 PR，只给完整门禁足够时间，不删减命令或断言。
- 微信小店的旧有效 AppID/AppSecret/CallbackToken 已可对应，但旧环境没有 V3 加密回调所需独立 AES；不复用其他应用 AES，不降低验签。配置页面须如实显示缺失引用，尚不启用该 Provider。

## 23:30 新增侧边栏独立板块

- 用户交接 `docs/plans/2026-09-07-sidebar-standard-parity-handoff.md` 已纳入，正式范围见 `09-sidebar-standard-parity.md`。先完成全仓静态库存、侧边栏源码链与生产只读差异检查，再派实现；原样复用标准版 UI，通过最小 Host/Adapter 接 V3，删除侧边栏聊天入口和请求，不取消存档后端。
- 旧全部 2,123 个跟踪文件、V3 1,828 个均读取成功；14 能力包与路由/服务/测试候选映射已落盘。深入运行验收不覆盖全仓，边界在 evidence/README 披露。
- 当前测试客户 OneID 和同开放平台 UnionID 正确，旧 8 份答卷未在 V3 同客户/历史投影中存在，保持后续数据迁移范围，不伪报身份修复。手机号徽章回读、画像四字段、可领取券目录、商品封面等确认缺口进入本 PR。
- 当前不启动第四个开发任务；群运营执行者 #186 收口后优先接侧边栏，问卷 #187 完成后接 Runner。配置中心继续现任务。迁移 0101/0102 已分别由群运营/配置占用，Sidebar 编码前由主任务核定下一个号。

## 2026-09-08 08:30 审核与调度

- 共享主线已更新到 PR #191 `82a7d4094a3f1292865b6319d2fc2e6e59c05646`，采用 fast / compile 前置检查和 Chromium 自动发现，必跑跳过不算通过。
- 群运营 #186 `d609eb6fa69346f97328cb41d18bbfd87ed1a1b6`、问卷外推 #187 `89404bc4b4e4cc93d9a5e8cb4a5dea7ad47f3e08`、配置中心 #190 `04b93dc4fd5f62b57593648c98074623e983ce04` 已变基该主线，完整 Linux CI 进行中。旧 HEAD 的绿色结果不能替代新 HEAD。
- 问卷外推已移除未装配的重复 Adapter，实际冻结 Host 保持唯一入口；群运营 geometry 回归按标准供体真实边距修复。配置与群运营在配置类型/TTL 装配的合并冲突已在独立只读用途检出预演定位，合入时保留两个能力的字段与校验。预演分支不发布、不建立总集成 PR。
- 侧边栏 Draft #192 初稿未通过审核。需修复切换客户后的上下文失效、OAuth 401 自动恢复、SDK 明确成功/未知结果、迟到回调、重复点击防护；恢复原回归断言。独立审计发现 bootstrap 字段遗漏、金额字段、分页及周期天数错误，必须依据领域 Port 修复，不能靠 UI 填零掩盖。修正前不合并或上线。
- 存档已完成真实 SDK 拉取/解密与生产启用；官方通知和入库尚无记录，详见 03。不将启用状态等同于通知驱动闭环完成。
- Runner 执行者继续最小不可变 action snapshot、既有 claim/续约/完成与未知结果恢复，迁移预留 0104；不在验收中发起真实 Codex 任务。实际执行端定位依赖仍须明确。


## 2026-09-08 09:45 审核与合并进展

| 板块 | 实现与测试证据 | 审核／合并／运行边界 |
| --- | --- | --- |
| 问卷外推 | #187 准确 HEAD `89404bc4b4e4cc93d9a5e8cb4a5dea7ad47f3e08` 全套 CI `34173097337` 通过；包含真实 PG 与实际冻结 Host Chromium | 已审核并合并为 `7085192b0f6510ba71da119ffa2989849066e1cd`；main CI `34174857627` 全部测试通过，部署上传仍在进行，启用目标配置须待安装回读后执行 |
| 配置中心 | #190 准确 HEAD `bb4e02a88b15667faa4e48d701f97612dd1e56e5` 全套 CI `34176163994` 通过 | 已审核并合并为 `a20a8b94b4dbde3041ba2131e274be1f04f240c6`，主线发布尚未完成。截图二次复核发现公众号首页开关键名与后端不一致，独立小 PR 修复；详情字段与发布接口仍可使用，不把该遗漏隐藏为已验收 |
| 群运营 | #186 新 HEAD `3b24c42d298c2cb997a04d6264eb2d3ff9d4084c` 保留配置主线；完整 jsdom 标准页面流程、全仓 compile、PG Composition 通过 | owner picker 已映射真实本地 staff_id；Linux 完整 CI 待通过。Mac Chromium DevTools 启动失败明确不算通过 |
| 侧边栏 | #192 标准 renderer 与可靠 V3 协调器适配继续修正；独立后端 bootstrap/profile/102 答卷分页修复已纳入工作区 | 初稿仍未通过整板块审核；须保留 OAuth/SDK/切客户/重复点击及完整浏览器回归，禁止因修复 UI 回退已经工作的企微身份链 |
| 运营 Runner | #193 `d8c1f35de49a459056bac85ff98a1d068621de51` 的本地 PG/race 与 fast/compile 通过 | 复审要求修正实际 required bindings 匹配与长任务持续心跳；需新准确 HEAD。实际执行端未明确，不安装到猜测的项目或伪造在线状态 |
| AI 调用入口 | 生产 OAuth2、REST capability catalog、MCP 六工具读取均 HTTP 200 | 业务计划 0、发送 0；这是 CRM 入口验证，上游实际 AI 服务接通与人工业务验收仍单列 |

执行任务发生模型容量失败后，已切换用户允许的 Sol medium 接续原工作区，保留已有代码和测试；不重建项目、不混并能力 PR。


### 09:56 生产回读与发布后异常

问卷 #187 已实际部署在 `7085192`，两项目标配置启用后 API/effects-worker 回读一致，管理员 operations 返回真实目录和 provider_enabled=true / local_only=false；未实际推送。

主线 CI `34174857627` 的程序安装及健康检查成功，但部署后 HXC 核对步骤失败，最终 conclusion=failure。只读证据：inspect 与 apply 均成功处理 2,904 条源记录、apply replay_verified=2,904，计数恒等式成立、active 身份重复键=0；同小时 scheduled run 关联的成功投影已被后续 apply 标记 superseded。脚本只读取 status=published，误把旧幂等成功快照当成空值，并保护性关闭 HXC 身份写入。需独立修复核对脚本，验证成功 run 关联的不可变投影与当前发布投影，保留真正异常时停写；不能换 run key 或删门禁。HXC 写入恢复前继续如实列为未收口。


### HXC 核对脚本受控恢复

独立修复 #195 准确 HEAD `b6ea55e1f94c49bb89679c6790628d8907795cfc` 已审阅，故障复现测试、HXC 边界与安装合同检查通过。生产先只读核对三份 run 关联快照均 succeeded 且计数有效、当前 published 投影唯一；随后持有发布锁执行准确摘要脚本，保留配置备份和失败停写 trap，未修改已安装版本内容。执行 exit=0：total=2904、replay_verified=2904、new_customers=0、subjects_delta=0、observations_delta=0、conflicts_delta=0；HXC 身份写入恢复。原 CI34174857627 仍保留 failure，不将现场恢复写成原流水线已通过；#195 全套 CI/合并仍须独立完成。


## 2026-09-08 10:20 收口复核

- 群运营 #186 当前审核 HEAD `fc8b068cba668571045d948d633f3718d8c04741`，已恢复目录读取必需的 ContactSecret 配置校验；不会要求只读目录打开发送开关。真实生产只读核对：12 paused、1 archived，runs、executions、intents、provider tasks、group-message effects 及存活群任务均为 0。启用前再次核对同一组条件，不激活历史计划或发送测试消息。
- Runner #193 当前审核 HEAD `a9b64e8e6eb026ad25b3df55729c94b1ec23bed7`，已修正必需 binding 全集匹配、长任务持续心跳和先续约后探测的时间预算；原 Codex initialize fixture 的断管回归已局部修复，相关 PG/race 重复运行通过。完整 CI 仍在进行，实际执行机器与项目尚未确定。
- 配置公众号开关修复 #194 `f240e05cc542ca3d23a07f6951f5a96ed879df21` 保留企微成功发布场景，并新增 OAuth 成功发布与完整快照保持测试；不能用新增测试替换既有断言。
- 侧边栏 #192 `606fc7ad2e29ef4c44e13d49f2649209d852b8f6` 复审要求补齐浏览器画像保存后的数据库终态、接受发送意图响应未知时稳定幂等键及客户隔离，并移除核心 Host 的 ts-nocheck。审核未通过前不合并，不以当前 CI 状态掩盖审查发现。
- 配置中心主线 `a20a8b9` 的 CI `34177502328` 全部测试及 check 成功；部署在安装前由主任务取消，避免重复执行已确认有缺陷的旧 HXC 核对脚本。实际生产仍为 `7085192`。待 #195 全套 CI 及合并后发布安全主线，原取消结论保留。


### 10:45 精确审核与生产配置补核

- #195 `b6ea55e1` 的 CI `34178623673` 全部通过，准确合并为 `0c7c0ee6b878cb414c38d88c0911a0f93a7fdb02`。主线保护 `strict=true`，必须在最新 main 上通过 `check`；未绕过保护。#194 `f240e05` 全绿后因 BEHIND 拒绝合并，已仅合入上述已审核主线为 `3d4892f968e2e3390739ef3c38250e39da9ba892`，新 CI `34180269062` 进行中。
- #186 `fc8b068` / CI `34179083927` 与 #193 `a9b64e8` / CI `34178801864` 全部 required 检查通过。因 strict 主线更新，仍须按顺序更新后再合并；不把旧基线绿色套用到新 HEAD。
- #192 二次独立审查发现真实服务端同键 replay 不返回一次性 grant，原 mock 错误假设可再次执行；另有无 focus/visibility 的切客户和 SDK 在途回执归属问题。已停止该路径的发布，要求发送前验证当前客户、发送与回执锁定原作用域、无 grant 重放如实待确认，不盲目生成新键或新凭据。现网不含本次未发布实现。
- 问卷配置补核发现 7 个旧版已启用的源问卷有唯一 V3 migration source_map，但 V3 operation 配置仍为 version=0/disabled。已在保护脚本内按真实 endpoint 摘要确认两个受控目标引用，仅输出源 ID/V3 ID/目标引用；接下来经正常管理员接口恢复配置，禁止历史答卷重推。本条核对不涉及历史数据导入。


问卷级配置补齐结果：上述 7 个唯一来源映射已通过正常 HTTP/CSRF/CAS 成功保存并回读 version=1，旧业务参数保留。前后真实外推快照、测试快照和非历史投递记录均为 0；具体证据与边界见 06。全局开关与逐问卷绑定现均已启用，生产正常新提交的实际收件由人工验收。
