# 配置开关错位：独立问题记录

来源：用户于 2026-09-08 在实施过程中新增反馈，附当前页面与期望样式两张图。

页面：配置中心 → AI 与自动化，例如“启用 AI 助手界面”“启用 AI 助手发送意图”。

## 问题

当前开关白色圆点与蓝色轨道的垂直位置不一致，圆点偏下、视觉倾斜，并挤占旁边的说明文字空间。

实际页面只读检查确认：现用组件为 V3 配置页的 `.cc-switch` / `.cc-slider`；轨道为 40×22 px，圆点伪元素却为 22×22 px，顶部偏移 3 px，开启时水平位移 22 px，导致下方与右侧均溢出。应修复现用组件的尺寸与样式冲突。

## 期望与验收

- 参考用户第二张图：水平胶囊形轨道，白色圆点垂直居中。
- 开启时蓝底白点靠右，关闭时中性底色白点靠左；两种状态圆点均处于轨道内部，内边距一致。
- 轨道和圆点尺寸固定，不受表格行高、说明文字换行或缩放影响。
- 开关与说明文字保留间距；在正常桌面宽度和窄屏中均不重叠。
- 禁用状态清楚，保留键盘操作和焦点反馈。

## 范围与状态

- 用户新增的明确样式修复项，单独登记，合入本轮交易与 UI 业务块；不扩展为全站视觉改版。
- 状态：本地实现并集成（`38f040b`），本地真实页面开启与关闭状态复测通过；尚未部署，禁用状态及窄屏待随候选版本完成复测。
- 本地页面实际计算结果：轨道 40×22 px、白点 16×16 px、顶部和左侧留白 3 px；开启位移 18 px，关闭位移 0，圆点均完整位于轨道内。修复位于持久 V3 配置 Host，冻结供体保持不变。
- 原始截图由本会话保留；不将包含其他页面内容的截图复制进仓库。
