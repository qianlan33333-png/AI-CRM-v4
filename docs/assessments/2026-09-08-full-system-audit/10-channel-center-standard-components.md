# 渠道码中心与全站标准组件（2026-09-08）

状态：用户追加本轮必修，正在对照标准版实施；尚未部署。

## 用户明确要求

1. 渠道名称与编码等编辑内容在维度切换后保留，保存方式以标准版为准，不把丢失内容当成未保存的正常行为。
2. 普通二维码与渠道获客链接使用标准版类型选择和条件展示，各自只显示对应配置。
3. 客服分配恢复标准版按比例 / 满额切换、人员卡片和添加客服；所有选客服入口使用同一标准组件，显示姓名及企微 ID。
4. 欢迎语使用标准版话术 + 素材组件，包含客户名变量、图片、小程序、附件、选群及实时预览，不使用素材 ID 输入框替代。
5. 入渠标签使用标准版按组搜索、选择和回显组件，不另造下拉替代品。
6. 修复所有维度保存 HTTP 409，验证首次创建、后续更新、版本回读、重复点击及冲突恢复，不移除并发校验。
7. 扫描 V3 全部话术素材、标签、群、客服选择入口，统一接入标准组件。

## 实现方式

- 标准供体：AI-CRM，固定 SHA `dd8d60dd8ddb983aca2ec88cc9e65a9f7563f79f`。
- 原组件 HTML / CSS / JS 原样复用；V3 数据与保存协议在外围 Host 适配，组件不重复实现。
- 先检查 V3 已复用的标准资源，合并调用入口，避免按页面复制多套组件。
- 公共组件及加载/打包文件指定唯一集成人；渠道专属状态/保存由渠道负责人实施，姓名目录由既有目录负责人实施。
- 此项为用户最新明确追加，覆盖此前对这几类 UI 统一的延期；其他治理和 P2 范围不扩张。

## 分类与验收

- OneID：组件不新建或猜测身份，读取已有资源并保留其所属标识；涉及客户触达仍复用 OneID。
- 持久化：渠道保存使用领域事务、版本和幂等；维度切换草稿保留不构成已保存到服务端。
- 外部效果：仅选择与编辑不触达；二维码生成/渠道链接等外部写继续经 Outbound / External Effects。
- 验收需要标准供体字节一致证明、全部调用页面清单、真实 DOM 选择/取消/回显/加载失败/重试，以及保存后重新打开的数据回读。

## 线上复现

- 新建渠道输入名称/编码后切到渠道载体，再返回基础配置，两项均清空。
- 重新填写测试名称“上线验收0908-渠道维度保留”和编码 `launch0908-channel-draft`，仅保存一次，页面跳回列表；本次没有出现用户报告的 409。
- 列表按测试名称搜索为空，尚未确认创建记录（需排查搜索分页与保存回读）。不要再次创建同编码来试错；先核对已存在记录。
- 未生成二维码、获客链接，未添加客服或发送欢迎语。
