schema: 1
capability: PR08-operation-cycle-lifecycle
source_repository: qianlan33333-png/AI-CRM-v2
source_path: /tmp/aicrm-v2-audit.yN3jmr
source_commit: 6bfbe5816bb89913c70adaca87d6a486260e016e
target_base_commit: 19384b9
target_branch: codex/import-operation-cycles
transfer_mode: constrained-domain-preparation

scope:
  included:
    - operation-cycle strategy definitions and immutable version snapshots
    - local run records and snapshot revisions
    - action stage contracts and queued/claimed/thread-bound/turn-started terminal states
    - strategy draft/active/paused/archived lifecycle semantics
    - runner compatibility and heartbeat acceptance facts
    - strategy-change proposal pending/accepted/rejected facts
    - transaction-bound local idempotency, event, and delivery seams
    - exact frozen cycles page and run-dossier templates
  excluded:
    - customer, external-user, mobile, tenant, segment, audience, campaign, or recipient identity
    - customer/segment/audience selection, marking, unmarking, enrollment, or recipient execution
    - WeCom/Provider calls, credentials, tokens, outbound effects, delivery sends, and retries
    - Store, PostgreSQL schema, generated queries, migrations, and historical import/runtime adapters
    - HTTP/auth/composition wiring, OpenAPI, central webshell, deployment, CI, and dependency manifests

behavior_contract:
  entities:
    strategy:
      identity: local strategy key, title, current lifecycle status, monotonically increasing version, definition JSON, snapshot JSON, and update timestamp
      statuses: draft, active, paused, archived; archived is terminal
      transitions: draft may activate/pause/archive; active may pause/archive; paused may activate/archive; same-status retries are idempotent
      validation: key max 120 bytes; title max 200 runes; version >= 1; definition/snapshot must be valid JSON and contain no excluded scope
    run:
      identity: local run key, strategy key, snapshot revision, snapshot JSON, and received timestamp
      validation: run key max 160 bytes, strategy key max 120 bytes, revision >= 1, valid JSON, no excluded scope
    runner:
      identity: runner/principal IDs, connector/Codex versions, compatibility status, strategy binding keys, and last heartbeat
      compatibility_statuses: ready, incompatible, unavailable
      heartbeat: local compatibility/lease fact only; no customer, OneID, order, entitlement, or Provider identity
    action_request:
      stages: queued -> claimed -> thread_bound -> turn_started -> completed
      failure: queued/claimed/thread_bound/turn_started may terminally fail; terminal stages cannot move to another stage
      fields: local strategy/run/action keys, strategy version, runner lease/thread/turn IDs, result/failure facts, and timestamps
    proposal:
      statuses: pending, accepted, rejected
      decision: pending has no decision actor/time; accepted/rejected requires both
  local_commands:
    - report accepts operation_cycle_snapshot.v1 with idempotency and records a local fact; replay does not append another event
    - start queues a local action request and records a queued fact; it does not send or execute an external effect
    - claim and heartbeat maintain runner compatibility and local action lease facts only
    - action events accept thread_bound, turn_started, completed, and failed; completed outcome_unknown is terminal and is never auto-retried
    - strategy context/index and run/action reads are local read contracts; filters must not contain excluded data-plane keys
    - strategy-change proposals use operation_cycle_strategy_change_proposal.v1 and local pending/decision facts
  limits_and_validation:
    keys: UTF-8, non-empty, no tabs/newlines; page limit 1..100 and offset 0..10000
    forbidden_scope: customer/external-user/mobile/tenant/segment/audience/campaign/recipient, OneID identifiers, order/product/entitlement/membership/subscription/service-period identifiers, credentials/secrets, file URLs, and non-empty external_effects
    json_digest: canonical JSON digest treats equivalent numeric forms consistently and rejects trailing/invalid JSON
  transaction_and_effects:
    mutation: Store fact/idempotency result, operation-cycle event, delivery acceptance, audit, and outbox must share the caller UoW when Terra supplies persistence
    provider_boundary: this donor has no WeCom/Provider/LLM call, customer selection, recipient execution, or retry; future external writes go through outbound only

backend:
  copied_and_adapted:
    - source: internal/operationcycle/port/port.go
      target: internal/operationcycle/port/port.go
      disposition: exact
      notes: pure strategy/run/runner/action/proposal value contracts and action statuses
    - source: internal/operationcycle/app/service.go
      target: internal/operationcycle/app/service.go
      disposition: adapted
      notes: v3 platform UoW and local operation-cycle event seam; fail-closed excluded-scope validation
    - source: internal/operationcycle/app/service_test.go
      target: internal/operationcycle/app/service_test.go
      disposition: adapted
      notes: idempotent report replay, outcome_unknown terminal recording, and forbidden-scope characterization
  added_v3_adaptation:
    - path: internal/operationcycle/domain/cycle.go
      reason: explicit lifecycle, stage, version, and excluded-identity invariants without customer IDs
    - path: internal/operationcycle/domain/cycle_test.go
      reason: characterization of strategy/action transitions and scope rejection
    - path: internal/operationcycle/port/events.go
      reason: local fact/event delivery seam; Terra adapts it to the versioned v3 event/outbox ports
    - path: internal/operationcycle/README.md
      reason: ownership, safety boundary, and Terra handoff record
  deferred_to_terra:
    - source: internal/operationcycle/store/repository.go
      reason: v3 operationcycle-owned schema, transaction receipts, locking, and generated SQL are not authorized here
    - source: internal/operationcycle/store/fact_delivery.go
      reason: event delivery must be adapted to the v3 transaction/outbox implementation
    - source: internal/operationcycle/store/queries/operation_cycles.sql
      reason: migration and SQLC generation are forbidden in this preparation branch
    - source: internal/operationcycle/store/generated/*
      reason: generated artifacts follow the authorized v3 schema and toolchain
    - source: internal/operationcycle/app/observation_history.go
      reason: V1 historical observations require independent import/version and ownership decisions
    - source: internal/operationcycle/app/static_history.go
      reason: archived static history is read-only and requires its own v3 history schema
    - source: internal/operationcycle/port/observation_history.go
      reason: history reader/writer contract is outside the current lifecycle slice
    - source: internal/operationcycle/port/static_history.go
      reason: history reader/writer contract is outside the current lifecycle slice
    - source: cmd/aicrm/legacy_operation_cycle_api.go
      reason: central auth/HTTP/composition wiring is forbidden; handler methods are retained as contract evidence only

frontend:
  byte_exact_staged_under: web/donors/operation-cycles-v2/src
  exact_file_count: 2
  exact_files:
    - source: web/src/admin/templates/cycles.html
      target: web/donors/operation-cycles-v2/src/admin/templates/cycles.html
      notes: byte-exact frozen v2 template; no wording, inline style, or interaction changes
    - source: web/src/admin/templates/cyclesDetail.html
      target: web/donors/operation-cycles-v2/src/admin/templates/cyclesDetail.html
      notes: byte-exact frozen v2 run-dossier template; no wording, inline style, or interaction changes
  donor_readme: web/donors/operation-cycles-v2/README.md
  template_boundary:
    cycles.html: frozen task-list presentation; raw action/detail hooks are evidence only and must be wired through a v3 adapter
    cyclesDetail.html: frozen read-only run dossier; its audience, target-count, send-time, delivery, and recipient-oriented labels are retained for byte evidence but are excluded from PR08 integration
  integration: donor templates are mounted only by the v3-owned operationCyclesAdapter through the existing PR10 webshell/primary sidebar; the adapter binds typed v3 operationcycle reads and captures the donor's existing primary button for the authorized host-side write Adapter before dynamically starting the unchanged donor main -> legacy -> AdminController chain; no second navigation shell, direct raw-route deployment, replacement renderer, mock fallback, or donor-file edit
  css_ts_and_dependencies:
    disposition: deferred-to-terra
    notes: no standalone cycles-specific TS/CSS exists in the donor; the already frozen PR01 shared main/legacy/AdminController runtime is reused byte-for-byte. The v3 host adapter installs typed loadDb and minimal authorized write bindings before importing donor main; it does not copy or modify donor runtime files or render new UI.
    contract_evidence:
      - web/src/admin/controller.ts
      - web/src/api/capabilities.ts
      - web/src/shared/api/client.ts
      - web/src/shared/api/types.ts
      - web/src/shared/api/mockData.ts
  historical_frontend_deferred:
    paths:
      - web/src/api/generated/p4-static-history/p4-static-history.ts
      - web/src/api/staticHistory.ts
      - web/src/admin/sections/staticHistory.ts
    notes: the broad V1 static-history UI remains excluded; PR08 exposes only typed local strategy-version and run-snapshot history through its v3 Admin API

exclusion_audit:
  excluded_data_plane:
    - customer and external-user identity, OneID, mobile, tenant, segment, audience, campaign, and recipient IDs
    - order, product, membership, subscription, entitlement, and service-period IDs or entitlement state
    - customer selection/marking/unmarking, enrollment, delivery recipient execution, and provider-send effects
  raw_template_only:
    - cyclesDetail.html contains donor labels for 人群, 发送, and delivery metrics; these must not be bound to customer/order/entitlement data in v3
  mixed_dependencies_not_copied:
    - web/src/admin/controller.ts
    - web/src/api/capabilities.ts
    - web/src/shared/api/client.ts
    - web/src/shared/api/types.ts
    - web/src/shared/api/mockData.ts
    - web/src/api/generated/p4-static-history/p4-static-history.ts
    - web/src/api/staticHistory.ts
    - web/src/admin/sections/staticHistory.ts

request_contract:
  source: cmd/aicrm/legacy_operation_cycle_api.go
  disposition: contract-evidence-only
  operations:
    - list/get strategy definitions
    - list/get run records
    - start and inspect local action requests
    - service-authenticated action claim and heartbeat
    - action event recording, including terminal outcome_unknown without automatic retry
    - strategy context read and strategy-change proposal create/list/decide
    - runner report acceptance
  notes: v3 HTTP/auth/OpenAPI routes and central registration remain Terra work; this commit does not wire or expose them

terra_followups:
  - owner: Terra
    item: define operationcycle-owned v3 tables, migrations, SQLC queries, locking/CAS, and transactional idempotency receipts
  - owner: Terra
    item: adapt local event/delivery seams to the v3 versioned event and outbox ports
  - owner: Terra
    item: implement authenticated HTTP/OpenAPI contracts without customer/audience/campaign/recipient execution
  - owner: Terra
    item: decide whether V1 observation/static history is a separate read-only import and expose it only after schema/ownership review
  - owner: Terra
    item: connect any real WeCom write only through outbound with attempted/executed/outcome_unknown/reconciled receipts; never direct from operationcycle
  - owner: Terra
    item: select the exact cycles frontend templates and build a narrowed TS/API adapter after removing shared excluded-domain branches

verification:
  go:
    - go test -count=1 ./internal/operationcycle/...
    - go vet ./internal/operationcycle/...
    - go test -race -count=1 ./internal/operationcycle/...
  frontend:
    - compare both exact templates with the frozen donor using cmp
    - verify source/target hashes in docs/donor-manifests/pr08-operation-cycles.sha256
  manifest: parse docs/donor-manifests/pr08-operation-cycles.yaml before commit

forbidden_touched:
  - cmd/aicrm
  - api/openapi.yaml
  - migrations
  - go.mod
  - go.sum
  - package*.json
  - deploy
  - CI
  - internal/platform
  - internal/access
  - internal/webshell
