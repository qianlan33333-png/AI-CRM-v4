# 历史事件：5538 / 0206 预备机迁移阻塞

状态：已归档，仅作审计背景。本文不是操作手册；其中涉及的一次性 `retry-staging` 入口已退出日常发布流程，不应复用。

提交 `5538d615a9abe2e25be799936866a7330b1d3af8` 在预备机执行迁移前，被整库 `pg_dump` 连接配置/权限故障阻断。历史专用恢复方案曾绑定该 SHA、单一 0206 迁移、预备机 loopback 数据库、旧约束读回、无目标回执/备份和一次性 guard。它把本可丢弃并重建的预备机合成库当成必须先备份的资产，导致发布队列停滞和额外工具修补。

当前规则按受保护机器角色固定：预备机只存合成数据，迁移失败后重建，不做整库备份；生产库含真实数据，只有生产迁移前备份。历史收据和提交保留在 Git 历史中；旧入口、旧账本和失败证据不删除、不改写为成功。

后续核查如需还原当时细节，应从该事件原始提交、PR 与发布收据中只读取证；不得用本归档执行新发布或重新消费旧候选。
