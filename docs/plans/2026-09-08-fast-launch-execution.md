# 快速上线执行记录

用户已确认实施限定范围首发。实施基线为 main `ae536c572dd93ccec5fe394ad715520ba88c1787`；标准行为供体 `dd8d60dd8ddb983aca2ec88cc9e65a9f7563f79f` 只读。

## 三个业务块

1. 客服工作台：商品及优惠券卡片发送、标签目录与客户打去标、客户筛选和字段投影、局部错误、统一入口与退出。
2. 交易和既有业务：保存反馈、重复提交、版本冲突、部分成功恢复、订单全量搜索和支付渠道；问卷、渠道、素材、原始 PDF、客户存档及群运营回归。
3. 动态文本：逐客户生成，人工审核/修改/拒绝，既有出站执行和结果回读。动态卡片延期。

全部治理专项、历史数据迁移、PDF 增强、HXC 行动、广域 REST/MCP、全局存档搜索、大批量导出及 P2 美化延期。B01/B02/B13/B16/B17/B18 继续延期。HXC 首发只读，开放平台仅已有六项 operation。

## 执行边界

- 原工作区有其他任务改动，本轮使用独立工作树，不覆盖这些改动。
- 最多三个业务 PR，本地闭环后集中审查，沿用已有必要 CI 和生产发布路径。
- 客服、动态复杂实现使用 terra xhigh；交易与 UI 使用 terra high；有边界测试可用 luna max；复杂交叉判断限极少量 sol medium。
- 主代理负责调度、集成与 Computer Use 实测。公共适配文件指定单一负责人。
- 客户、发送目标和动态触达读取/解析既有 OneID，不隐式建客；纯定义和界面不制造身份依赖。
- PostgreSQL 业务状态、幂等、审计和效果接受在必要时同一 UoW 提交。企微写复用 Outbound/External Effects；模型调用复用外部效果机制，网络不占数据库事务。
- 不新增身份、队列或对账内核；结果未知不换键重试；SDK 执行成功不等于送达。
- 冻结供体不改，只通过 V3 Host/overlay 适配。

## 验收

- 侧边栏六入口、测试标签全生命周期及企微回读、商品和券卡真实收件。
- 三笔支付实付共 0.05 元：0.02 全退；0.02 分两次各退 0.01；0.02 商品使用 0.01 券支付 0.01 后退款。用户完成资金最终确认。
- 测试商品不关联真实课程交付或生产外推；实际测试身份不写入本记录。
- 指定测试客户的固定话术、动态文本审核后发送，以及指定测试群既有能力。
- 失败、超时、重复点击、部分成功、版本冲突和未知结果。
- 本地测试、数据库测试、模拟浏览器、线上客户端及 Provider 结果分别记录。候选包完整，线上版本一致，确认现有回退方式。

## 状态

- 已完成主分支核对、隔离工作树、固定 Node/npm 依赖安装及独立 PostgreSQL 16 测试库准备。
- 三块主要实现已进入本地集成分支。素材响应丢失恢复、侧边栏未知结果跨刷新防重及标签未决操作顺序保护均已修复并完成专项测试；用户后续追加群运营、渠道标准组件、标签真实执行、订单检索、商品维度、优惠券发布/选择、素材上传及 OneID 前端移除，均纳入同一首发候选。
- 客户筛选、客服目录、标签状态回显、普通商品公共封面、订单搜索与详情、问卷分享发布状态、人工审核动态文本及两项用户追加 UI 调整均已有局部验证。静态页真实退出按钮复用既有 Webshell 会话与 CSRF；没有 current-principal API 时仅显示“当前登录账号”。
- 较早候选前端主测试 444 通过、0 失败，额外 12 项 Host 专项通过；群运营中文工作路径解析已修复，随后局部测试和旧候选发布包检查通过。以上结果不覆盖后续新增改动，收口后必须重新执行候选检查。
- 候选 `9be1c64` 完成串行 Go 全量 PostgreSQL 测试。首次只有四项 Open Platform journey 因 Unix-socket DSN 无 authority 被测试 URL 解析器拒绝，改为带 authority 的同一 Unix-socket DSN 后四项均通过。其余包完成；本地九项需 Linux Chromium 的 journey 跳过，仍须既有 CI 验证，不能算作通过。
- 较早候选快速前置检查通过。追加改动按模块进行 Go/Host 回归；标准原件的依赖和 passive 资源必须一并进入发布包，不能以 web/dist 本地可用替代完整发布闭包。
- 用户已完成过内置浏览器 CRM 登录，测试商品、券草稿和已发布测试问卷已准备。企微应用访问仍被工具权限拒绝，没有真实发送或支付退款结果。
- 未发布；仍待所有追加问题完成实现、完整候选发布包及必要 CI、发布版本核对、真实收件、标签回读、问卷提交和资金验收。线上正常配置页面尚未显示本轮新 AI 生成配置，模型凭据/模型实际有效态尚未验证。

## 追加问题的集成状态

- 群运营目录读取与发送开关分开，企微姓名/群目录从现有读取适配器投影；刷新失败须可见，不能将写入本地目录当作外发成功。
- 渠道保存使用打开表单时读到的版本；冲突保留草稿，不能保存前重新读最新版本后覆盖他人修改。渠道及优惠券以标准原始模板/脚本为供体，协议转换在外围。
- 标签目录业务写需要明确写入能力；读目录授权不能隐式打开新增、改名或删除。发布后仍须测试标签的官方回读。
- 订单手机号/外部联系人查询仅 Resolve 既有 OneID；无匹配返回空页，冲突不能退化为全量。暂不支持身份筛选导出时明确阻止。日期显示只转换原始时间一次。
- 商品购买后动作已接入既有支付完成事件的不可变动作快照和认证付款会话读取，企微打标走现有 Customer TagCommand 与 Outbound/External Effects。支付后刷新是同一订单的只读恢复；定向 PostgreSQL 回放、防重复和归属检查通过，仍未进行真实资金验收。
- 优惠券发布缺幂等标识已修复；原件编辑器以同源外部脚本加载并完成延迟初始化、保存/发布与响应丢失恢复测试。商品选择为原件弹窗，不再要求填写商品 ID；仍待生产草稿发布回读。
- 图片与 PDF 附件原冻结表单 multipart DOM 旅程及真实 PostgreSQL 上传/读回测试通过；生产原生文件选择与上传仍待实测。
- OneID 独立前端入口已在候选移除，后端身份领域仍保留。

## 最近候选检查

- 前端主测试 444 项通过；后续订单检查有一项旧客户 ID 断言，已改为新外部联系人查询契约并通过定向测试。余下 Host 与发布资源检查全部通过。普通商品素材与标签启用状态随后修正，最终候选需要包含这些修改后的检查结果。
- 商品标签选择必须保存显式 `enabled`；开启和关闭保留选中标签的 DOM 请求断言已通过。服务端启用配置的严格校验及周期商品/问卷入口正在复核。
- 标签目录真实写入增加受控发布配置步骤，默认关闭；启用前验证现有企微、目录读取、联系客户凭据与 Outbound 配置，沿用唯一 CI 发布路径。配置脚本的失败保留原配置测试通过，尚未在生产启用。
- 本地完整 Go PostgreSQL 检查的共享数据库资源失败已逐项串行重跑；支付后动作的两项旧旅程断言已更新并定向通过。正在运行最终串行 race 套件，不能将早先失败的整轮记录写成通过。
